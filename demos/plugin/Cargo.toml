[package]
name = "plugin"
version = "0.7.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
intuicio-data = { version = "0.7", path = "../../platform/data" }
intuicio-core = { version = "0.7", path = "../../platform/core" }
intuicio-derive = { version = "0.7", path = "../../platform/derive" }
intuicio-frontend-simpleton = { version = "0.7", path = "../../frontends/simpleton" }
