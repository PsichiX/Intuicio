[package]
name = "plugin"
version = "0.30.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
intuicio-data = { version = "0.30", path = "../../platform/data" }
intuicio-core = { version = "0.30", path = "../../platform/core" }
intuicio-derive = { version = "0.30", path = "../../platform/derive" }
intuicio-frontend-simpleton = { version = "0.30", path = "../../frontends/simpleton" }
