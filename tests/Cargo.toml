[package]
name = "tests"
version = "0.46.2"
edition = "2024"
publish = false

[dependencies]
intuicio-data = { version = "0.46", path = "../platform/data" }
intuicio-core = { version = "0.46", path = "../platform/core" }
intuicio-derive = { version = "0.46", path = "../platform/derive" }
intuicio-framework-text = { version = "0.46", path = "../frameworks/text" }
gc = { version = "*", features = ["derive"] }
