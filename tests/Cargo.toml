[package]
name = "tests"
version = "0.44.3"
edition = "2021"
publish = false

[dependencies]
intuicio-data = { version = "0.44", path = "../platform/data" }
intuicio-core = { version = "0.44", path = "../platform/core" }
intuicio-derive = { version = "0.44", path = "../platform/derive" }
intuicio-framework-text = { version = "0.44", path = "../frameworks/text" }
gc = { version = "*", features = ["derive"] }
