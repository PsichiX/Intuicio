[package]
name = "tests"
version = "0.3.2"
edition = "2021"
publish = false

[features]
bench = []
all = []

[dev-dependencies]
intuicio-data = { version = "0.3", path = "../data" }
intuicio-core = { version = "0.3", path = "../core" }
intuicio-derive = { version = "0.3", path = "../derive" }
intuicio-backend-vm = { version = "0.3", path = "../backends/vm" }
intuicio-frontend-vault = { version = "0.3", path = "../frontends/vault" }
rune = "*"
rune-modules = "*"
rhai = "*"