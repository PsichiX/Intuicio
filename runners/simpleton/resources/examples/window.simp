mod example_window {
    import "simpleton_window.plugin";

    func main(args) {
        var window = window::new(window::WindowConfig {
            title: "Simpleton window example",
            vsync: true,
        });
        while window::maintain(window) {}
    }
}